# GitHub recent activity

Summarize recent activity (last 48 hours) across all GitHub repos (use `gh`) by understanding the code changes themselves, not just the commit logs. Clear an concise, grouped by project.